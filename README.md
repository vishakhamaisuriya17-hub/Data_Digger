# Data_Digger
SQL Database Management Project demonstrating PostgreSQL operations using Customers, Orders, Products, and OrderDetails tables. Includes table creation, data insertion, updating, deletion, filtering, sorting, foreign keys, joins, aggregate functions like SUM, MAX, MIN, AVG, COUNT, and date-based queries for practical database management.
